# Empty input is a deliberately tiny malformed smoke fixture.  It exercises the
# public container entrypoint's no-panic / stable-rejection path without making
# the every-PR corpus depend on a codec encoder.
id = "container-empty-input-001"
file = "../fixtures/smoke/empty-input.webp"
sha256 = "e3b0c44298fc1c149afbf4c8996fb92427ae41e4649b934ca495991b7852b855"
class = "MustReject"
source = "generated"
license = "CC0-1.0"
codec = "Container"
features = ["empty-input", "public-api", "no-panic"]
max_work_units = 1
max_alloc_bytes = 0
notes = "M0 baseline: `read_info`, one-shot decode, and incremental finish must reject without allocating."
